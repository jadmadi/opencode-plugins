# data-analytics spec

Status: scaffold. The `/data-analytics` command reports status only. The real
surface is undecided and waits on the skills in the plugin repo's `tmp/`.

Map-level facts: one status command, no tools, no limits, no environment
overrides. The delivered copy lives at
`docs/compose/spec/data-analytics.md` in
[jadmadi/opencode-data-analytics](https://github.com/jadmadi/opencode-data-analytics)
and grows there first.
